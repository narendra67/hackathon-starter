extends ../layout

block content
    .page-header
        h3 Create New Sport

    form.form-horizontal(method='POST', action="/createSport")
        input(type='hidden', name='_csrf', value=_csrf)
        .form-group
            label(class='col-sm-2 control-label', for='name') Name
            .col-sm-8
                input.form-control(type='text', name='name', id='name', autofocus=true)
        .form-group
            label(class='col-sm-2 control-label', for='type') Type
            .col-sm-8
                input.form-control(type='text', name='type', id='type')
        .form-group
            .col-sm-offset-2.col-sm-8
                button.btn.btn-primary(type='submit')
                    i.fa.fa-envelope
                    | Create Sport
    br
    br
    br
    form(method="POST", action="/createSports")
        input(type='hidden', name='_csrf', value=_csrf)
        button.btn.btn-primary(type='submit')
            i.fa.fa-envelope
            | Create Sports